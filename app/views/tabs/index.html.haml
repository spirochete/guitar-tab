%h1 Guitar Tabs

#whatever
  = form_tag tabs_path, :method => :get do
    %p
      = text_field_tag :search, params[:search]
      = hidden_field_tag :username, params[:username]
      = submit_tag "Search", :name => nil


- if !@tabs.nil?
  %table
    %tr
      %th Artist
      %th Song
      %th Tuning
      %th Tabbed By
      %th URL

  
    - @tabs.each do |tab|
      %tr
        %td= link_to tab.artist, "/index/#{tab.artist}"
        %td= tab.song
        %td= tab.tuning
        - if tab.user.present?
          %td= link_to tab.user.username, "/profile/#{tab.user.username}"
        - else
          %td
        %td= link_to "open", tab.submission.url
- else
  %p No tabs found.